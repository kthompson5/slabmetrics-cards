---
import { getAllCards } from "../utils/paths.ts";
const cards = getAllCards();
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>SlabMetrics Cards</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root{ --sm-green:#12B886 }
      body{ font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial; margin:0; padding:20px }
      a{ color: var(--sm-green) }
    </style>
  </head>
  <body>
    <h1>SlabMetrics Cards</h1>
    <ul>
      {cards.map(c => <li><a href={`/cards/${c.id}/`}>{c.player} — {c.set} (Grade {c.grade_overall ?? ''})</a></li>)}
    </ul>
  </body>
</html>
